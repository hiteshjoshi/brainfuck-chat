[package]
name = "trainfuck"
version = "0.1.0"
edition = "2021"
description = "A Trainfuck interpreter - Brainfuck with networking extensions for 1:1 chat"

[dependencies]
thiserror = "1.0"
clap = { version = "4.0", features = ["derive"] }

[[bin]]
name = "trainfuck"
path = "src/main.rs"
